package Lab02.a;

public class Application {
    public void draw() {
        DrawingCanvas drawingCanvas = new DrawingCanvas();
//        drawingCanvas.setDrawStrategy(new DrawStrategyCircle());
        drawingCanvas.draw();
    }
}
